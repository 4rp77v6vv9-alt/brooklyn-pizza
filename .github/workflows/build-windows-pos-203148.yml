name: Build Brooklyn Pizza Windows POS 20.31.48

on:
  push:
    branches:
      - build/windows-pos-20.31.48
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-windows:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: windows-pos-build
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: windows-pos-build/package.json

      - name: Install dependencies
        run: npm install

      - name: Syntax check
        shell: powershell
        run: |
          node --check electron/main.cjs
          node --check electron/preload.cjs
          node --check electron/config.cjs
          node --check electron/hardware.cjs

      - name: Build NSIS installer
        run: npm run dist:win

      - name: Upload Windows installer
        uses: actions/upload-artifact@v4
        with:
          name: Brooklyn-Pizza-POS-Setup-20.31.48
          path: windows-pos-build/dist/*.exe
          if-no-files-found: error
          retention-days: 7
